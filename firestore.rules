service cloud.firestore {
  match /databases/{database}/documents {
    match /addresses/{document=**} {
    	allow read;
      allow create: if request.auth.uid != null;
    }
    match /addresses/{createdBy} {
      allow update, delete: if request.auth.email == createdBy;
    }
  }
}